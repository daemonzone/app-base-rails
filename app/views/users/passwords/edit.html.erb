<div class="container">
  <div class="row">
    <div class="col-lg-12 mb-4 mx-auto">

      <h2 class="text-center m-4">Cambio password</h2>
      
      <div class="col-8 offset-2 col-sm-6 offset-sm-3 mb-4">
        <p class="mb-4 text-center">
          <strong>Scegli subito la tua nuova password</strong> per accedere al Portale AVQ!
          <br/>Clicca quindi il pulsante <strong>Cambia password</strong> e ricorda di non condividere con nessuno la tua nuova password.</p>
        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>
          <%= f.hidden_field :reset_password_token %>

          <div class="field mt-3">
            <label for="password"><h5>Password</h5></label>
            <%= f.password_field :password, autofocus: true, placeholder: "Nuova password" + (@minimum_password_length ? " (Lunghezza minima: #{@minimum_password_length} caratteri) ": ''), class: 'form-control mb-3' %>
          </div>

          <div class="field">
            <label for="password"><h5>Conferma Password</h5></label>
            <%= f.password_field :password_confirmation, placeholder: "Ripeti la nuova password", class: 'form-control mb-3' %>
          </div>

          <div class="actions">
            <%= f.submit "Cambia password", class: 'btn btn-primary btn-block mb-3' %>
          </div>
        <% end %>
      </div>

    </div>
  </div>
</div>